spring:
  jpa:
    open-in-view: 'false'
    show-sql: 'true'
    hibernate:
      ddl-auto: update
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/securitydemo
    password: 47444744

server:
  port: 8040